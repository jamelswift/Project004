# System Dashboard - ระบบควบคุมเซ็นเซอร์ IoT

## 🎯 ภาพรวม

**System Dashboard** เป็นหน้าเว็บสำหรับจัดการเซ็นเซอร์ IoT และควบคุมอุปกรณ์ output ผ่านเงื่อนไขอัตโนมัติ ออกแบบมาให้รองรับเซ็นเซอร์ 3 ชนิดและพร้อมสำหรับการเชื่อมต่อฮาร์ดแวร์จริงในอนาคต

### เซ็นเซอร์ที่รองรับ:
1. **เซ็นเซอร์อุณหภูมิและความชื้นอากาศ** → อุณหภูมิ (°C) + ความชื้นอากาศ (%)
2. **เซ็นเซอร์วัดความเข้มแสง** → ความเข้มแสง (Lux)
3. **เซ็นเซอร์วัดความชื้นในดิน** → ความชื้นดิน (%)

---

## 📍 เข้าถึงหน้า

```
URL: http://localhost:3000/admin/system
สิทธิ์: Admin เท่านั้น
```

---

## 🎨 ส่วนประกอบหลัก

### 1️⃣ ส่วนสถานะระบบ
```
📊 สถานะระบบ
├─ โหมดระบบ: "ข้อมูลจำลอง" (mock mode)
├─ Backend: เชื่อมต่อแล้ว
├─ Real-time: ใช้งาน
└─ อัปเดตล่าสุด: HH:MM:SS
```

### 2️⃣ ส่วนแสดงข้อมูลเซ็นเซอร์
```
🌡️ เซ็นเซอร์ - แสดง 3 การ์ด
├─ Card 1: อุณหภูมิ/ความชื้นอากาศ
│   ├─ อุณหภูมิ: 28.5°C
│   ├─ ความชื้น: 65%
│   └─ สถานะ: ✓ ใช้งาน | ข้อมูลจำลอง
│
├─ Card 2: ความเข้มแสง
│   ├─ ความเข้มแสง: 450 lux
│   └─ สถานะ: ✓ ใช้งาน | ข้อมูลจำลอง
│
└─ Card 3: ความชื้นดิน
    ├─ ความชื้นดิน: 55%
    └─ สถานะ: ✓ ใช้งาน | ข้อมูลจำลอง
```

### 3️⃣ ส่วนควบคุม LED
```
💡 ระบบควบคุม LED - 2 ดวง
├─ LED ดวงที่ 1
│   ├─ สถานะ: ปิด
│   ├─ ควบคุมโดย: ผู้ใช้ (Manual)
│   └─ ปุ่ม: เปิด
│
└─ LED ดวงที่ 2
    ├─ สถานะ: เปิด
    ├─ ควบคุมโดย: เงื่อนไขอัตโนมัติ (Rule)
    ├─ ชื่อเงื่อนไข: "ทำให้ LED เปิดเมื่ออุณหภูมิสูง"
    └─ ปุ่ม: ปิด
```

### 4️⃣ ส่วนระบบเงื่อนไขอัตโนมัติ
```
⚡ ระบบเงื่อนไขอัตโนมัติ
├─ ปุ่ม: "+ เพิ่มเงื่อนไข"
├─ แบบฟอร์มสร้างเงื่อนไข:
│   ├─ ชื่อเงื่อนไข: [Text input]
│   ├─ เลือกเซ็นเซอร์: [Dropdown]
│   ├─ คุณสมบัติ: [Dropdown]
│   ├─ เงื่อนไข: [>, <, =]
│   ├─ ค่า: [Number input]
│   ├─ เลือก LED: [Dropdown]
│   ├─ การกระทำ: [on/off]
│   └─ ปุ่ม: "สร้างเงื่อนไข"
│
└─ รายการเงื่อนไขที่ใช้งาน:
    ├─ เงื่อนไข 1
    │   ├─ ชื่อ: "ทำให้ LED เปิดเมื่ออุณหภูมิสูง"
    │   ├─ เงื่อนไข: sensor_001 (temperature) > 30 → LED 1 ON
    │   ├─ สถานะ: เปิดใช้งาน
    │   ├─ สัมผัส: 12 ครั้ง | ครั้งล่าสุด: HH:MM:SS
    │   ├─ ปุ่ม: Power | Delete
    │   
    └─ ... (เงื่อนไขอื่น)
```

### 5️⃣ ส่วนแสดงสถานะเรียลไทม์
```
🕐 สถานะเรียลไทม์ (10 เหตุการณ์ล่าสุด)
├─ [TrendingUp] 📈 อัปเดตเซ็นเซอร์
│   └─ "อุณหภูมิเพิ่มขึ้นเป็น 28.5°C" | HH:MM:SS
│
├─ [Zap] ⚡ เงื่อนไขถูกสัมผัส
│   └─ "เงื่อนไข 'ทำให้ LED เปิดเมื่ออุณหภูมิสูง' ถูกสัมผัส" | HH:MM:SS
│
├─ [Lightbulb] 💛 เปลี่ยนสถานะ LED
│   └─ "LED ดวงที่ 2 เปลี่ยนสถานะเป็น เปิด โดยเงื่อนไขอัตโนมัติ" | HH:MM:SS
│
└─ ... (เหตุการณ์อื่น)
```

---

## 📊 โครงสร้างข้อมูลจำลอง (Mock Data)

### Sensors
```json
{
  "sensors": [
    {
      "id": "sensor_001",
      "type": "temperature_humidity",
      "name": "เซ็นเซอร์อุณหภูมิและความชื้นอากาศ",
      "temperature": 28.5,
      "humidity": 65,
      "unit": { "temperature": "°C", "humidity": "%" },
      "timestamp": "2024-01-08T14:30:00Z",
      "isActive": true
    },
    {
      "id": "sensor_002",
      "type": "light",
      "name": "เซ็นเซอร์วัดความเข้มแสง",
      "illuminance": 450,
      "unit": "lux",
      "timestamp": "2024-01-08T14:30:00Z",
      "isActive": true
    },
    {
      "id": "sensor_003",
      "type": "soil_moisture",
      "name": "เซ็นเซอร์วัดความชื้นในดิน",
      "moisture": 55,
      "unit": "%",
      "timestamp": "2024-01-08T14:30:00Z",
      "isActive": true
    }
  ]
}
```

### LED Statuses
```json
{
  "leds": [
    {
      "id": "led_1",
      "name": "LED ดวงที่ 1",
      "isOn": false,
      "source": "manual",
      "lastChangedBy": "ผู้ใช้",
      "timestamp": "2024-01-08T14:30:00Z"
    },
    {
      "id": "led_2",
      "name": "LED ดวงที่ 2",
      "isOn": true,
      "source": "rule",
      "lastChangedBy": "ชุมชนอัตโนมัติ: อุณหภูมิสูง",
      "timestamp": "2024-01-08T14:30:00Z"
    }
  ]
}
```

### Automation Rules
```json
{
  "rules": [
    {
      "id": "rule_001",
      "name": "ทำให้ LED เปิดเมื่ออุณหภูมิสูง",
      "sensorId": "sensor_001",
      "sensorType": "temperature_humidity",
      "sensorProperty": "temperature",
      "condition": ">",
      "targetValue": 30,
      "targetLedId": "led_1",
      "targetLedAction": "on",
      "isActive": true,
      "createdAt": "2024-01-07T14:30:00Z",
      "lastTriggered": "2024-01-08T14:25:00Z",
      "triggerCount": 12
    }
  ]
}
```

---

## 🔄 API Mock Functions

### `systemMockApi`

| Function | คำอธิบาย |
|----------|---------|
| `getSensors()` | ดึงข้อมูลเซ็นเซอร์ทั้งหมด |
| `getSensor(id)` | ดึงเซ็นเซอร์เฉพาะตัว |
| `getLedStatuses()` | ดึงสถานะ LED ทั้งหมด |
| `toggleLed(id, isOn)` | เปลี่ยนสถานะ LED |
| `getAutomationRules()` | ดึงเงื่อนไขทั้งหมด |
| `createRule(rule)` | สร้างเงื่อนไขใหม่ |
| `deleteRule(id)` | ลบเงื่อนไข |
| `getSystemStatus()` | ดึงสถานะระบบ |
| `getSystemEvents()` | ดึงเหตุการณ์เรียลไทม์ |
| `simulateSensorUpdate(...)` | จำลองการอัปเดตเซ็นเซอร์ |

---

## ⚙️ พฤติกรรม

### 🔄 Auto-refresh
- ข้อมูลจะอัปเดตโดยอัตโนมัติทุก **5 วินาที**
- ใช้ `setInterval()` เพื่อโหลดข้อมูลใหม่

### 📡 Network Simulation
- Mock API จำลอง network delay (200-500ms)
- ให้ประสบการณ์ใกล้เคียงกับ API จริง

### 🎯 Real-time Events
- เหตุการณ์ไม่ขึ้นอยู่กับการสัมผัสเงื่อนไขจริง
- ใช้เพื่อสาธิตการใช้งาน

### 🔐 Authorization
- ตรวจสอบ admin role ในแต่ละหน้า
- ถ้าไม่มีสิทธิ์ → redirect ไปหน้าหลัก

---

## 💡 คุณสมบัติพิเศษ

### ✅ อ้างอิงเซ็นเซอร์เป็นรายตัว
```typescript
// ❌ ไม่ผูกกับเซ็นเซอร์รวม
// ✅ ลิงก์ไปยังเซ็นเซอร์เดี่ยว
{
  sensorId: "sensor_001",      // เซ็นเซอร์เฉพาะ
  sensorProperty: "temperature" // คุณสมบัติเฉพาะ
}
```

### ✅ รองรับการเพิ่มเซ็นเซอร์
```typescript
// สามารถเพิ่มเซ็นเซอร์ sensor_004, sensor_005... โดยไม่แก้ไข UI
// ประเภท: SensorType = "temperature_humidity" | "light" | "soil_moisture" | "..."
```

### ✅ ภาษาไทยทั้งหมด
- UI components
- Button labels
- Menu items
- Error messages
- Comments

---

## 🎬 การใช้งาน: สร้างเงื่อนไข

### ตัวอย่าง 1: เปิด LED เมื่ออุณหภูมิสูง
```
1. คลิก "เพิ่มเงื่อนไข"
2. ชื่อ: "เปิด LED เมื่ออุณหภูมิสูง"
3. เซ็นเซอร์: "เซ็นเซอร์อุณหภูมิและความชื้นอากาศ"
4. คุณสมบัติ: "อุณหภูมิ (°C)"
5. เงื่อนไข: ">" (มากกว่า)
6. ค่า: "30"
7. LED: "LED ดวงที่ 1"
8. การกระทำ: "เปิด (ON)"
9. คลิก "สร้างเงื่อนไข"
```

### ตัวอย่าง 2: ปิด LED เมื่อแสงเพียงพอ
```
1. คลิก "เพิ่มเงื่อนไข"
2. ชื่อ: "ปิด LED เมื่อแสงเพียงพอ"
3. เซ็นเซอร์: "เซ็นเซอร์วัดความเข้มแสง"
4. คุณสมบัติ: "ความเข้มแสง (lux)"
5. เงื่อนไข: ">" (มากกว่า)
6. ค่า: "500"
7. LED: "LED ดวงที่ 2"
8. การกระทำ: "ปิด (OFF)"
9. คลิก "สร้างเงื่อนไข"
```

---

## 📁 โครงสร้างไฟล์ (Complete)

```
frontend/
├── types/
│   ├── index.ts
│   └── system.ts                    ← ประเภท System
├── lib/
│   ├── api.ts
│   ├── auth.ts
│   ├── aws-iot.ts
│   ├── notifications.ts
│   ├── simulator.ts
│   ├── utils.ts
│   ├── weather.ts
│   └── system-mock-api.ts           ← Mock API Service
├── components/
│   ├── system/                      ← System Components
│   │   ├── index.ts
│   │   ├── system-status-panel.tsx
│   │   ├── sensor-data-card.tsx
│   │   ├── led-control-panel.tsx
│   │   ├── automation-panel.tsx
│   │   └── realtime-event-list.tsx
│   ├── navbar.tsx (updated)         ← เพิ่มลิงก์ /admin/system
│   └── ... (components อื่น)
└── app/
    └── admin/
        ├── page.tsx
        └── system/
            └── page.tsx              ← System Dashboard
```

---

## 🚀 การสลับเป็นข้อมูลจริง (Future)

### 1. แทนที่ Mock API
```typescript
// ปัจจุบัน: lib/system-mock-api.ts
// ในอนาคต: lib/system-api.ts

const systemApi = {
  getSensors: async () => {
    const res = await fetch('/api/sensors')
    return res.json()
  },
  // ... functions อื่น
}
```

### 2. เปลี่ยน System Page
```typescript
// import { systemMockApi } from "@/lib/system-mock-api"
import { systemApi } from "@/lib/system-api"  // ใช้ API จริง

// const [sensors] = await systemApi.getSensors()
```

---

## 🧪 Testing

### Test Cases
- ✅ โหลดหน้า (authorization)
- ✅ แสดงข้อมูล mock
- ✅ อัปเดต auto-refresh
- ✅ ควบคุม LED (toggle)
- ✅ สร้างเงื่อนไข
- ✅ ลบเงื่อนไข
- ✅ แสดง real-time events

---

## 📝 หมายเหตุ

- 🔴 **ปัจจุบัน**: ใช้ข้อมูลจำลองเท่านั้น
- 🟢 **พร้อม**: เชื่อมต่อกับ Backend API จริง
- 🟡 **ในอนาคต**: WebSocket สำหรับ real-time ที่แท้จริง

---

## 📞 ติดต่อ

- 📧 Repo: jamelswift/Project004
- 🌐 Branch: main
- 📂 Path: frontend/app/admin/system
