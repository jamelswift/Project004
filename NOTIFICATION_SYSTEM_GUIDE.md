# 📢 ระบบแจ้งเตือน (Notification System)

## ภาพรวม

ระบบแจ้งเตือนอัตโนมัติเมื่อค่าเซ็นเซอร์เกินขีดจำกัด (Threshold-based Notification System)

## 🏗️ โครงสร้าง

### Backend (server.ts)

#### 1. ฟังก์ชันตรวจสอบ Threshold
```typescript
checkThresholdAndCreateAlert(deviceId, sensorType, value)
```
- ตรวจสอบค่าเซ็นเซอร์เทียบกับขีดจำกัด
- สร้าง notification อัตโนมัติถ้าค่าเกิน
- บันทึกลงฐานข้อมูลและ in-memory store

#### 2. API Endpoints

**POST /api/sensors** (แก้ไขแล้ว)
- ฟังก์ชั่น: อัปเดตค่าเซ็นเซอร์และตรวจสอบ threshold
- Request:
  ```json
  {
    "sensorId": "SENSOR_001",
    "deviceId": "ESP32_001",
    "sensorType": "temperature",
    "value": 32.5
  }
  ```
- Response:
  ```json
  {
    "success": true,
    "sensor": {...},
    "thresholdCheck": {
      "alertCreated": true,
      "notification": {...}
    }
  }
  ```

**POST /api/thresholds**
- ฟังก์ชั่น: สร้างค่าขีดจำกัดใหม่
- Request:
  ```json
  {
    "deviceId": "ESP32_001",
    "sensorType": "temperature",
    "minValue": 15,
    "maxValue": 35,
    "enabled": true
  }
  ```

**GET /api/thresholds/device/:deviceId**
- ฟังก์ชั่น: ดึงค่าขีดจำกัดของอุปกรณ์

**GET /api/alerts/device/:deviceId**
- ฟังก์ชั่น: ดึงการแจ้งเตือนของอุปกรณ์

**GET /api/alerts/unread**
- ฟังก์ชั่น: ดึงการแจ้งเตือนที่ยังไม่อ่าน

**PUT /api/alerts/:id/read**
- ฟังก์ชั่น: ทำเครื่องหมายว่าอ่านแล้ว

**DELETE /api/alerts/:id**
- ฟังก์ชั่น: ลบการแจ้งเตือน

**POST /api/alerts/test** (สำหรับทดสอบ)
- ฟังก์ชั่น: สร้าง alert สำหรับทดสอบ
- Request:
  ```json
  {
    "deviceId": "ESP32_001",
    "sensorType": "temperature",
    "value": 40,
    "message": "อุณหภูมิสูงเกินไป",
    "level": "critical"
  }
  ```

### Frontend Components

#### 1. NotificationCenter (notification-center.tsx)
- แสดงรายการการแจ้งเตือน
- ทำเครื่องหมายว่าอ่านแล้ว
- ลบการแจ้งเตือน
- ขออนุญาต Browser Notification

#### 2. ThresholdSettings (threshold-settings.tsx)
- สร้างค่าขีดจำกัดใหม่
- แก้ไขค่าขีดจำกัด
- ลบค่าขีดจำกัด
- เลือกประเภทเซ็นเซอร์ (temperature, humidity, light)

#### 3. AlertsPage (/app/dashboard/alerts/page.tsx)
- หน้าหลักสำหรับจัดการการแจ้งเตือน
- แสดง tabs สำหรับการแจ้งเตือนและค่าขีดจำกัด

## 🔧 วิธีใช้งาน

### 1. สร้างค่าขีดจำกัด

```bash
POST http://localhost:5001/api/thresholds
Content-Type: application/json

{
  "deviceId": "ESP32_001",
  "sensorType": "temperature",
  "minValue": 15,
  "maxValue": 35,
  "enabled": true
}
```

### 2. อัปเดตค่าเซ็นเซอร์ (ทำให้เกิด threshold check)

```bash
POST http://localhost:5001/api/sensors
Content-Type: application/json

{
  "sensorId": "TEMP_001",
  "deviceId": "ESP32_001",
  "sensorType": "temperature",
  "value": 38
}
```

Response (หากเกิน threshold):
```json
{
  "success": true,
  "sensor": {...},
  "thresholdCheck": {
    "alertCreated": true,
    "notification": {
      "id": "alert-...",
      "deviceId": "ESP32_001",
      "sensorType": "temperature",
      "message": "🚨 temperature เกินขีดจำกัด: ค่า 38 เกินกว่า 35",
      "level": "critical",
      "value": 38,
      "time": "2024-...",
      "isRead": false
    }
  }
}
```

### 3. ดึงการแจ้งเตือน

```bash
GET http://localhost:5001/api/alerts/unread
```

### 4. ทำเครื่องหมายว่าอ่านแล้ว

```bash
PUT http://localhost:5001/api/alerts/{alertId}/read
```

### 5. ทดสอบระบบ

```bash
POST http://localhost:5001/api/alerts/test
Content-Type: application/json

{
  "deviceId": "ESP32_001",
  "sensorType": "temperature",
  "value": 40,
  "message": "Test Alert - ทดสอบระบบแจ้งเตือน",
  "level": "critical"
}
```

## 🎨 Frontend UI

### หน้า Alerts (/dashboard/alerts)

**Tab 1: การแจ้งเตือน**
- รายการการแจ้งเตือนทั้งหมด
- สีแบ่งตามระดับความสำคัญ:
  - 🔴 critical (แดง)
  - 🟠 warning (ส้ม)
  - 🔵 info (น้ำเงิน)
- ปุ่มทำเครื่องหมายว่าอ่านแล้ว
- ปุ่มลบ
- นับการแจ้งเตือนที่ยังไม่อ่าน

**Tab 2: ค่าขีดจำกัด**
- สร้างค่าขีดจำกัดใหม่
- เลือกอุปกรณ์ (deviceId)
- เลือกประเภทเซ็นเซอร์
- ตั้งค่าต่ำสุดและสูงสุด
- เปิด/ปิด threshold
- รายการค่าขีดจำกัดที่มีอยู่

## 📊 ตัวอย่างค่าขีดจำกัดแนะนำ

| ประเภทเซ็นเซอร์ | ค่าต่ำสุด | ค่าสูงสุด | หน่วย |
|-----------------|---------|---------|------|
| temperature | 15 | 35 | °C |
| humidity | 30 | 80 | % |
| light | 50 | 5000 | Lux |
| PM2.5 | 0 | 50 | µg/m³ |

## 🔄 ขั้นตอนการทำงาน

1. **สร้าง Threshold**
   - ผู้ใช้สร้างค่าขีดจำกัดสำหรับแต่ละเซ็นเซอร์

2. **ส่งข้อมูลเซ็นเซอร์**
   - อุปกรณ์ส่งค่าเซ็นเซอร์ไปยัง POST /api/sensors

3. **ตรวจสอบ Threshold**
   - ระบบตรวจสอบค่าเซ็นเซอร์เทียบกับค่าขีดจำกัด
   - ถ้าเกิน → สร้าง notification

4. **บันทึก Notification**
   - บันทึกลงใน in-memory database (db.notifications)
   - บันทึกลงใน DynamoDB (ถ้าใช้งาน)

5. **แสดงใน UI**
   - Frontend ดึงข้อมูลการแจ้งเตือนทุก ๆ 5 วินาที
   - แสดงรายการใน NotificationCenter

## 🧪 ทดสอบระบบ

### ใช้ curl ทดสอบ

```bash
# 1. สร้าง Threshold
curl -X POST http://localhost:5001/api/thresholds \
  -H "Content-Type: application/json" \
  -d '{
    "deviceId": "ESP32_001",
    "sensorType": "temperature",
    "minValue": 15,
    "maxValue": 35,
    "enabled": true
  }'

# 2. สร้าง Alert Test
curl -X POST http://localhost:5001/api/alerts/test \
  -H "Content-Type: application/json" \
  -d '{
    "deviceId": "ESP32_001",
    "sensorType": "temperature",
    "value": 40,
    "message": "อุณหภูมิสูงเกินไป",
    "level": "critical"
  }'

# 3. ดึงการแจ้งเตือน
curl http://localhost:5001/api/alerts/unread

# 4. ทำเครื่องหมายว่าอ่านแล้ว
curl -X PUT http://localhost:5001/api/alerts/{alertId}/read
```

### ใช้ Frontend

1. ไปที่ **Dashboard → Alerts**
2. ไปแท็บ **ค่าขีดจำกัด**
3. สร้างค่าขีดจำกัด (เช่น temperature 15-35°C)
4. ไปแท็บ **การแจ้งเตือน**
5. รีเฟรช (ปุ่มทดสอบส่งอยู่ใน Tab 1)
6. ดูการแจ้งเตือนปรากฏขึ้น

## 🚨 ข้อมูลการแจ้งเตือน

```typescript
interface Alert {
  id: string                          // ID ของ alert
  deviceId: string                    // ID อุปกรณ์
  sensorType: string                  // ประเภทเซ็นเซอร์
  message: string                     // ข้อความแจ้งเตือน (ภาษาไทย)
  level: 'critical' | 'warning' | 'info'  // ระดับความสำคัญ
  value: number                       // ค่าเซ็นเซอร์ที่เกิน
  time: string                        // เวลาสร้าง (ISO string)
  isRead: boolean                     // อ่านแล้ว?
}
```

## 📝 หมายเหตุสำคัญ

1. **ในหน่วยความจำ**: Notifications เก็บใน `db.notifications` (in-memory)
   - สูญหาย? Restart Server
   - ต้องเก็บนาน? ใช้ DynamoDB

2. **Real-time Updates**: Frontend ดึงข้อมูลทุก 5-30 วินาที
   - อาจไม่ real-time 100%
   - สำหรับ real-time ต้องใช้ WebSocket

3. **ระดับความสำคัญ**:
   - `critical`: สีแดง - ต้องใช้ความสนใจทันที
   - `warning`: สีส้ม - ควรตรวจสอบ
   - `info`: สีน้ำเงิน - ข้อมูลอ้างอิง

4. **Threshold Check**:
   - ถ้า `minValue` กำหนด → เช็คค่าต่ำสุด
   - ถ้า `maxValue` กำหนด → เช็คค่าสูงสุด
   - ถ้าเกิน → สร้าง notification อัตโนมัติ

## 🔗 ลิงก์ที่เกี่ยวข้อง

- Frontend: [/app/dashboard/alerts/page.tsx](./frontend/app/dashboard/alerts/page.tsx)
- Components:
  - [NotificationCenter](./frontend/components/notification-center.tsx)
  - [ThresholdSettings](./frontend/components/threshold-settings.tsx)
- Backend: [server.ts - Notification API](./backend/server.ts#L926-L1200)
- Libraries: [notifications.ts](./frontend/lib/notifications.ts)

## ✅ Checklist การตั้งค่า

- [x] สร้างฟังก์ชัน checkThresholdAndCreateAlert
- [x] แก้ไข POST /api/sensors ให้ตรวจสอบ threshold
- [x] สร้าง API endpoints สำหรับ threshold
- [x] สร้าง API endpoints สำหรับ alerts/notifications
- [x] สร้าง Frontend Components (NotificationCenter, ThresholdSettings)
- [x] สร้างหน้า /dashboard/alerts
- [x] ตรวจสอบ API URLs ใช้ port 5001
- [x] เพิ่ม test endpoint POST /api/alerts/test
- [x] เขียน Documentation

## 🚀 ขั้นตอนต่อไป

1. **Real-time Updates**: ใช้ WebSocket สำหรับการแจ้งเตือนแบบ real-time
2. **Persistent Storage**: บันทึก notifications ใน DynamoDB
3. **Email Alerts**: ส่งอีเมลเมื่อเกิด critical alerts
4. **Browser Notifications**: ใช้ Web Push API
5. **Alert History**: เพิ่มหน้าประวัติการแจ้งเตือนอย่างละเอียด
6. **Customization**: ให้ผู้ใช้เลือกช่องทางการแจ้งเตือน (Email, SMS, Push)

---

📧 **ติดต่อ**: สำหรับคำถามหรือปัญหาเกี่ยวกับระบบแจ้งเตือน
