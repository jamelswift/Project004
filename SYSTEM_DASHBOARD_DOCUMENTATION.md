# ระบบควบคุมเซ็นเซอร์ IoT - System Dashboard

## 📋 ภาพรวม

ระบบนี้เป็นหน้าเว็บ (System Dashboard) ที่ออกแบบมาเพื่อจัดการเซ็นเซอร์ IoT 3 ชนิด พร้อมตัวเลือกควบคุม LED และเงื่อนไขอัตโนมัติ ปัจจุบันใช้ข้อมูลจำลอง และพร้อมสำหรับการเชื่อมต่อหาร์ดแวร์จริงในอนาคต

## 🎯 วัตถุประสงค์

- สร้างหน้าเว็บระบบที่รองรับเซ็นเซอร์ทั้ง 3 แบบแยกจากกัน
- ใช้ข้อมูลจำลอง (Mock Data) เพื่อการทดสอบ
- เตรียมโครงสร้างรองรับการเชื่อมต่อฮาร์ดแวร์จริง
- ตัวอักษรและ UI ทั้งหมดเป็นภาษาไทย

## 📂 โครงสร้างไฟล์

```
frontend/
├── types/
│   └── system.ts                          # ประเภทข้อมูล (Types) สำหรับระบบ
├── lib/
│   └── system-mock-api.ts                 # API/Service จำลองข้อมูล
├── components/system/
│   ├── index.ts                           # Export ทุก components
│   ├── system-status-panel.tsx            # ส่วนสถานะระบบ
│   ├── sensor-data-card.tsx               # ส่วนแสดงข้อมูลเซ็นเซอร์
│   ├── led-control-panel.tsx              # ส่วนควบคุม LED
│   ├── automation-panel.tsx               # ส่วนระบบเงื่อนไขอัตโนมัติ
│   └── realtime-event-list.tsx            # ส่วนแสดงสถานะเรียลไทม์
└── app/admin/system/
    └── page.tsx                           # หน้า System Dashboard หลัก
```

## 🔧 ประเภทเซ็นเซอร์ที่รองรับ

### 1. เซ็นเซอร์อุณหภูมิและความชื้นอากาศ (Temperature & Humidity)
```typescript
{
  id: "sensor_001",
  type: "temperature_humidity",
  name: "เซ็นเซอร์อุณหภูมิและความชื้นอากาศ",
  temperature: 28.5,      // °C
  humidity: 65,           // %
  timestamp: "...",
  isActive: true
}
```

### 2. เซ็นเซอร์วัดความเข้มแสง (Light Sensor)
```typescript
{
  id: "sensor_002",
  type: "light",
  name: "เซ็นเซอร์วัดความเข้มแสง",
  illuminance: 450,       // Lux
  timestamp: "...",
  isActive: true
}
```

### 3. เซ็นเซอร์วัดความชื้นในดิน (Soil Moisture)
```typescript
{
  id: "sensor_003",
  type: "soil_moisture",
  name: "เซ็นเซอร์วัดความชื้นในดิน",
  moisture: 55,           // %
  timestamp: "...",
  isActive: true
}
```

## 🎨 ส่วนประกอบของหน้า

### 1. ส่วนสถานะระบบ (System Status Panel)
- **แสดงโหมดระบบ**: จำลองข้อมูล (mock) หรือข้อมูลจริง (real)
- **สถานะการเชื่อมต่อ Backend**: เชื่อมต่อแล้ว/ไม่เชื่อมต่อ
- **สถานะ Real-time**: ใช้งาน/ปิดใช้งาน
- **เวลาอัปเดตล่าสุด**: แสดงเวลาอัปเดตข้อมูล

### 2. ส่วนแสดงข้อมูลเซ็นเซอร์ (Sensor Data Grid)
- แสดง 3 การ์ดสำหรับแต่ละเซ็นเซอร์
- แสดงข้อมูลเรียลไทม์ของเซ็นเซอร์
- ป้ายกำกับ "ข้อมูลจำลอง"
- สถานะใช้งาน/ไม่ใช้งาน

### 3. ส่วนควบคุมอุปกรณ์ Output (LED Control Panel)
- ควบคุมได้ 2 ดวง (LED 1 และ LED 2)
- แสดงสถานะ: เปิด/ปิด
- แสดงแหล่งที่มา: Manual (ผู้ใช้) / Rule (เงื่อนไขอัตโนมัติ)
- แสดงชื่อเงื่อนไขที่ควบคุม

### 4. ส่วนระบบเงื่อนไขอัตโนมัติ (Automation Rules)
- สร้างเงื่อนไขโดยการ:
  - เลือกเซ็นเซอร์เฉพาะตัว
  - เลือกคุณสมบัติ (temperature, humidity, illuminance, moisture)
  - เลือกเงื่อนไข (>, <, =)
  - กำหนดค่า
  - เลือก LED ที่ต้องควบคุม
  - เลือกการกระทำ (on/off)
- แสดงรายการเงื่อนไขที่ใช้งาน
- สามารถเปิด/ปิดใช้งานแต่ละเงื่อนไข
- แสดงจำนวนครั้งที่เงื่อนไขเป็นจริง
- สามารถลบเงื่อนไข

### 5. ส่วนแสดงสถานะเรียลไทม์ (Realtime Events)
- แสดงเหตุการณ์: อัปเดตเซ็นเซอร์, เงื่อนไขถูกสัมผัส, เปลี่ยนสถานะ LED, ข้อผิดพลาด
- เรียงลำดับจากใหม่ไปเก่า
- แสดง timestamp ของแต่ละเหตุการณ์
- สีและไอคอนแตกต่างกันตามประเภทเหตุการณ์

## 📊 ข้อมูลจำลอง (Mock Data)

ทั้งหมดมาจากไฟล์ `lib/system-mock-api.ts`:

```typescript
// ตัวอย่างข้อมูลจำลอง
const mockSensors = [
  // อุณหภูมิ/ความชื้อ: 28.5°C, 65%
  // ความเข้มแสง: 450 lux
  // ความชื้นดิน: 55%
]

const mockLedStatuses = [
  // LED 1: ปิด, ควบคุมด้วยผู้ใช้
  // LED 2: เปิด, ควบคุมโดยเงื่อนไขอัตโนมัติ
]

const mockAutomationRules = [
  // เงื่อนไข 3 รายการ
]
```

## 🚀 การใช้งาน

### 1. เข้าถึงหน้า
- URL: `/admin/system`
- ต้องล็ออินด้วยบัญชี admin
- หากไม่มีสิทธิ์จะถูกเปลี่ยนเส้นทางไปหน้าหลัก

### 2. ตัวเลือก Tabs
- **เซ็นเซอร์**: ดูข้อมูลเซ็นเซอร์ทั้ง 3 ชนิด
- **LED**: ควบคุม LED 1 และ 2
- **เงื่อนไข**: สร้างและจัดการเงื่อนไขอัตโนมัติ
- **Real-time**: ดูเหตุการณ์เรียลไทม์

### 3. สร้างเงื่อนไขอัตโนมัติ
1. คลิก "เพิ่มเงื่อนไข"
2. กรอกชื่อเงื่อนไข
3. เลือกเซ็นเซอร์
4. เลือกคุณสมบัติและค่า
5. เลือก LED ที่ต้องควบคุม
6. คลิก "สร้างเงื่อนไข"

## 🔄 การอัปเดตข้อมูล

- ข้อมูลจะอัปเดตโดยอัตโนมัติทุก 5 วินาที
- ใช้ `Promise.all()` เพื่อโหลดข้อมูลแบบขนาน
- มีการจำลอง network delay (200-500ms)

## 💾 โครงสร้างข้อมูลสำหรับการเชื่อมต่อจริง

### API Endpoints ที่ต้องสร้าง:
```
GET  /api/sensors                    # ดึงเซ็นเซอร์ทั้งหมด
GET  /api/sensors/:id               # ดึงเซ็นเซอร์เฉพาะตัว
GET  /api/leds                      # ดึงสถานะ LED ทั้งหมด
POST /api/leds/:id/toggle           # สลับสถานะ LED
GET  /api/rules                     # ดึงเงื่อนไขอัตโนมัติทั้งหมด
POST /api/rules                     # สร้างเงื่อนไขใหม่
DELETE /api/rules/:id               # ลบเงื่อนไข
GET  /api/system/status             # ดึงสถานะระบบ
GET  /api/events                    # ดึงเหตุการณ์เรียลไทม์
```

## 🎯 การออกแบบ Features

### ✅ อ้างอิงเซ็นเซอร์เป็นรายตัว
- แต่ละเงื่อนไขอ้างอิง `sensorId` เฉพาะ
- ไม่ผูกกับเซ็นเซอร์รวม (grouped sensors)
- สามารถเลือกคุณสมบัติต่างกันจากเซ็นเซอร์เดียวกัน

### ✅ รองรับการเพิ่มเซ็นเซอร์ใหม่
- ประเภท: `type: SensorType` (extensible union)
- คุณสมบัติ: `sensorProperty: string` (flexible)
- สามารถเพิ่มเซ็นเซอร์ใหม่โดยไม่แก้ไข UI

### ✅ พร้อมสำหรับข้อมูลจริง
- ใช้ `system-mock-api.ts` เป็นตัวอย่าง
- สามารถแทนที่ด้วย API จริง
- โครงสร้าง types ตรงกับ backend

## 🌐 การภาษาไทย

- ✅ UI/Button/Menu: ภาษาไทย
- ✅ Comments: ภาษาไทย
- ✅ Labels/Placeholders: ภาษาไทย
- ✅ Error messages: ภาษาไทย
- ❌ ไม่มีคำศัพท์อังกฤษในส่วน UI

## 📝 หมายเหตุสำคัญ

1. **Mock Data**: ใช้ข้อมูลจำลองเท่านั้น ไม่เชื่อมต่อ ESP32 จริง
2. **Authorization**: เฉพาะ admin เท่านั้นที่สามารถเข้าถึงหน้านี้
3. **Real-time Updates**: อัปเดตข้อมูลทุก 5 วินาที
4. **Responsive Design**: เหมาะสมสำหรับมือถือและเดสก์ทอป
5. **Extensible**: รองรับการเพิ่มเซ็นเซอร์ LED และคุณสมบัติใหม่

## 🔗 เชื่อมโยง

- Admin Dashboard: `/admin`
- System Page: `/admin/system`
- Navbar: อัปเดตให้มีลิงก์ไปยัง System ด้วยปุ่ม "ระบบ"

## 🚧 ขั้นตอนถัดไป

1. เชื่อมต่อกับ Backend API จริง
2. เพิ่มเซ็นเซอร์ที่ 4+ ได้ตามต้องการ
3. ระบบ WebSocket สำหรับการอัปเดต real-time
4. บันทึก logs ของเหตุการณ์
5. การสำรองข้อมูล และ History
